# Подробная инструкция по развертыванию Doge DEX на GitHub Pages

Эта инструкция предназначена для тех, кто хочет создать свой собственный DEX на базе этого проекта.

## Шаг 1: Форк репозитория

1. **Перейдите на GitHub**: https://github.com/777genius/doge-dex
2. **Нажмите кнопку "Fork"** в правом верхнем углу
3. **Выберите свой аккаунт** как владельца форка
4. Подождите, пока GitHub создаст копию репозитория

## Шаг 2: Клонирование на локальную машину

```bash
# Замените YOUR_USERNAME на ваше имя пользователя GitHub
git clone https://github.com/YOUR_USERNAME/doge-dex.git
cd doge-dex
```

## Шаг 3: Установка зависимостей

Требования:
- Node.js версии 20 или выше
- Yarn package manager

```bash
# Установите зависимости
yarn install
```

## Шаг 4: Настройка конфигурации

### 4.1 Обновите файл `.env`

```bash
# Откройте файл .env в текстовом редакторе
nano .env  # или используйте VS Code, Sublime Text и т.д.
```

Измените следующие параметры:

```env
# Ваш Broker ID от Orderly Network
VITE_ORDERLY_BROKER_ID=your-broker-id

# Название вашего DEX
VITE_ORDERLY_BROKER_NAME=Your DEX Name
VITE_APP_NAME=Your DEX Name
VITE_APP_DESCRIPTION=Your DEX description

# Сеть (mainnet или testnet)
VITE_ORDERLY_NETWORK_ID=mainnet

# Настройки меню
VITE_ENABLED_MENUS=Trading,Portfolio,Markets,Swap,Leaderboard

# Логотип (если есть)
VITE_HAS_PRIMARY_LOGO=false
```

### 4.2 Обновите файл `public/config.js`

Этот файл позволяет изменять настройки без пересборки:

```javascript
window.__RUNTIME_CONFIG__ = {
  VITE_ORDERLY_BROKER_ID: "your-broker-id",
  VITE_ORDERLY_BROKER_NAME: "Your DEX Name",
  VITE_ORDERLY_NETWORK_ID: "mainnet",
  VITE_APP_NAME: "Your DEX Name",
  VITE_APP_DESCRIPTION: "Your DEX description",

  // Социальные сети (опционально)
  VITE_TELEGRAM_URL: "https://t.me/your_channel",
  VITE_DISCORD_URL: "https://discord.gg/your_server",
  VITE_TWITTER_URL: "https://twitter.com/your_account",

  // Меню
  VITE_ENABLED_MENUS: "Trading,Portfolio,Markets,Swap,Leaderboard",
};
```

### 4.3 Обновите `vite.config.ts`

Измените base path на название вашего репозитория:

```typescript
export default defineConfig(() => {
  // Замените doge-dex на название вашего репозитория
  const basePath = process.env.PUBLIC_PATH || "/your-repo-name/";

  return {
    base: basePath,
    // ...
  };
});
```

### 4.4 Добавьте свой логотип (опционально)

1. Подготовьте логотип в формате `.webp` (рекомендуемый размер: 200x80px)
2. Поместите файл в `public/logo.webp`
3. Для мобильной версии (опционально): `public/logo-secondary.webp`
4. Обновите в `public/config.js`:
   ```javascript
   VITE_HAS_PRIMARY_LOGO: "true",
   VITE_HAS_SECONDARY_LOGO: "true", // если есть вторичный логотип
   ```

## Шаг 5: Локальное тестирование

```bash
# Запустите dev сервер
yarn dev
```

Откройте браузер: http://localhost:5173

Проверьте:
- ✅ Правильно отображается название
- ✅ Логотип загружается (если добавлен)
- ✅ Все страницы работают
- ✅ Можно подключить кошелек

## Шаг 6: Настройка GitHub Pages

### 6.1 Обновите `.github/workflows/deploy.yml`

Проверьте, что файл содержит правильную настройку для вашей ветки:

```yaml
on:
  push:
    branches:
      - main  # или master, в зависимости от вашей основной ветки
```

### 6.2 Закоммитьте изменения

```bash
# Добавьте все изменения
git add .

# Создайте коммит
git commit -m "feat: configure DEX for my project"

# Запушьте в GitHub
git push origin main
```

## Шаг 7: Активируйте GitHub Pages

1. **Перейдите в Settings** вашего репозитория на GitHub
2. **Найдите раздел "Pages"** в левом меню
3. **В разделе "Source"** выберите **"GitHub Actions"**
4. **Сохраните настройки**

## Шаг 8: Дождитесь деплоя

1. Перейдите во вкладку **"Actions"** вашего репозитория
2. Вы увидите запущенный workflow **"Deploy to GitHub Pages"**
3. Дождитесь завершения (обычно 2-5 минут)
4. При успешном выполнении появится зеленая галочка ✅

## Шаг 9: Проверьте ваш сайт

Ваш DEX будет доступен по адресу:

```
https://YOUR_USERNAME.github.io/your-repo-name/
```

Например: `https://johndoe.github.io/my-dex/`

## Шаг 10: Регистрация Broker ID (важно!)

Чтобы ваш DEX работал с реальными торгами, вам нужно:

1. **Зарегистрируйтесь как Broker** в Orderly Network:
   - Посетите https://orderly.network
   - Свяжитесь с командой для получения Broker ID
   - Получите API ключи

2. **Обновите конфигурацию** с вашим реальным Broker ID:
   ```javascript
   VITE_ORDERLY_BROKER_ID: "your-real-broker-id",
   ```

3. **Пересоберите и задеплойте** проект

## Частые проблемы и решения

### Проблема: 404 ошибка при переходе по прямым ссылкам

**Решение**: Убедитесь, что файлы `public/404.html` и скрипт в `index.html` присутствуют

### Проблема: Ошибки в консоли от Amplitude API

**Решение**: Это нормально. Amplitude — встроенная аналитика Orderly SDK. Ошибки не влияют на работу DEX.

Чтобы отключить (необязательно):
```javascript
// В public/config.js добавьте:
VITE_DISABLE_ANALYTICS: "true",
```

### Проблема: Логотип не загружается

**Решение**:
1. Проверьте путь к файлу: `public/logo.webp`
2. Убедитесь, что в config.js установлено: `VITE_HAS_PRIMARY_LOGO: "true"`
3. Очистите кэш браузера (Ctrl+Shift+R)

### Проблема: Не работают роуты (URL)

**Решение**: Проверьте `basePath` в `vite.config.ts` - должен соответствовать названию репозитория

### Проблема: Не могу подключить кошелек

**Решение**:
1. Убедитесь, что используете правильный `VITE_ORDERLY_NETWORK_ID` (mainnet/testnet)
2. Проверьте, что ваш Broker ID активен

## Обновление проекта

Чтобы получить обновления из основного репозитория:

```bash
# Добавьте upstream репозиторий
git remote add upstream https://github.com/777genius/doge-dex.git

# Получите изменения
git fetch upstream

# Смержите изменения
git merge upstream/main

# Разрешите конфликты (если есть)
# Затем запушьте
git push origin main
```

## Полезные ссылки

- [Orderly Network Docs](https://orderly.network/docs/sdks)
- [GitHub Pages Docs](https://docs.github.com/en/pages)
- [Orderly Storybook](https://storybook.orderly.network/)

## Поддержка

При возникновении проблем:
1. Проверьте GitHub Actions logs
2. Проверьте консоль браузера (F12)
3. Создайте Issue в репозитории
4. Свяжитесь с Orderly Network для вопросов по Broker ID

---

**Удачи с вашим DEX! 🚀**
